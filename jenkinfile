steps {
// sh '''
// cd $WORKSPACE
// git init
// git remote remove origin
// git remote add origin http://$STASH_CRED_USR:$STASH_CRED_PSW@$STASH_IP:$STASH_PORT/scm/llaesb/automationtesting.git
// git pull origin master --allow-unrelated-histories
// '''
// }
